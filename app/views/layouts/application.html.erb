<!DOCTYPE html>
<html lang="fr" class="h-full">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>AppartAgent — Trouvez votre appartement idéal</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: {
              50: '#eff6ff', 100: '#dbeafe', 200: '#bfdbfe', 300: '#93c5fd',
              400: '#60a5fa', 500: '#3b82f6', 600: '#2563eb', 700: '#1d4ed8',
              800: '#1e40af', 900: '#1e3a8a'
            }
          }
        }
      }
    }
  </script>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application" %>
</head>
<body class="h-full bg-gray-50">
  <!-- Navigation -->
  <nav class="bg-white shadow-sm border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <a href="/" class="flex items-center space-x-2">
            <span class="text-2xl">🏠</span>
            <span class="text-xl font-bold text-primary-600">AppartAgent</span>
          </a>
          <div class="hidden md:flex ml-10 space-x-8">
            <a href="<%= how_it_works_path %>" class="text-gray-600 hover:text-primary-600 px-3 py-2 text-sm font-medium">Comment ça marche</a>
            <a href="<%= pricing_path %>" class="text-gray-600 hover:text-primary-600 px-3 py-2 text-sm font-medium">Tarifs</a>
          </div>
        </div>
        <div class="flex items-center space-x-4">
          <% if logged_in? %>
            <a href="<%= dashboard_path %>" class="text-gray-600 hover:text-primary-600 text-sm font-medium">Tableau de bord</a>
            <a href="<%= alerts_path %>" class="text-gray-600 hover:text-primary-600 text-sm font-medium">Alertes</a>
            <span class="text-sm text-gray-500"><%= current_user.name %></span>
            <%= button_to "Déconnexion", logout_path, method: :delete, class: "text-sm text-gray-500 hover:text-red-600" %>
          <% else %>
            <a href="<%= login_path %>" class="text-gray-600 hover:text-primary-600 text-sm font-medium">Connexion</a>
            <a href="<%= signup_path %>" class="bg-primary-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-primary-700">S'inscrire</a>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Flash messages -->
  <% if notice %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg text-sm"><%= notice %></div>
    </div>
  <% end %>
  <% if alert %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg text-sm"><%= alert %></div>
    </div>
  <% end %>

  <!-- Content -->
  <main>
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-gray-400 mt-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div>
          <h3 class="text-white font-bold text-lg mb-4">🏠 AppartAgent</h3>
          <p class="text-sm">L'assistant intelligent pour trouver votre appartement en France.</p>
        </div>
        <div>
          <h4 class="text-white font-semibold mb-4">Produit</h4>
          <ul class="space-y-2 text-sm">
            <li><a href="<%= how_it_works_path %>" class="hover:text-white">Comment ça marche</a></li>
            <li><a href="<%= pricing_path %>" class="hover:text-white">Tarifs</a></li>
          </ul>
        </div>
        <div>
          <h4 class="text-white font-semibold mb-4">Plateformes</h4>
          <ul class="space-y-2 text-sm">
            <li>SeLoger</li>
            <li>LeBonCoin</li>
            <li>PAP</li>
            <li>Bien'ici</li>
          </ul>
        </div>
        <div>
          <h4 class="text-white font-semibold mb-4">Légal</h4>
          <ul class="space-y-2 text-sm">
            <li><a href="#" class="hover:text-white">Mentions légales</a></li>
            <li><a href="#" class="hover:text-white">Politique de confidentialité</a></li>
            <li><a href="#" class="hover:text-white">CGU</a></li>
          </ul>
        </div>
      </div>
      <div class="border-t border-gray-700 mt-8 pt-8 text-center text-sm">
        <p>&copy; <%= Date.current.year %> AppartAgent. Tous droits réservés.</p>
      </div>
    </div>
  </footer>
</body>
</html>
